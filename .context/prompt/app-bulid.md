我想优化一下功能编排创建这个功能，我希望在界面的左侧添加两个功能

1. 系统prompt填写。默认状态下，会带有prompt，随着用户添加前置判断，以及意图识别中的技能，prompt会自动进行修改，但运营用户人为修正，修正后以用户手动修改的为准
2. 技能推荐。该功能会列举出不同行业分类下常用的技能，供用户进行选择，用户可以直接拖拽到意图识别功能区进行意图添加。添加后意图识别编辑页面里的字段会根据系统信息默认填写

